utils::globalVariables(c("read_id", "try_read_id", "wimpfiles",
                         "taxID", "phylum", "csvname", "barcode",
                         "coverage", "URL", "sampleID",
                         "CVTERMID", "mutationassociated"))
#to not get errors when building the package
